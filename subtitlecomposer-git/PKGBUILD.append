# pocketsphinx causes build errors

makedepends=(${makedepends[@]/pocketsphinx*})

optdepends=(${optdepends[@]/pocketsphinx*})


# Fix pkgver
pkgver() {
  cd "$srcdir/$_name"
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}
