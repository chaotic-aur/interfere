
depends=(${depends[@]/gtk2*} gtk3)

build() {
  cd "$pkgname-$pkgver"
  lazbuild -B "$pkgname/$pkgname.lpi" \
    --lazarusdir=/usr/lib/lazarus \
    --os=linux --cpu="$CARCH" \
    --bm=Release --ws=gtk3 
}
