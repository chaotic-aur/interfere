source=('git+https://github.com/AlexandreRouma/SDRPlusPlus')
md5sums=('SKIP')
depends+=(airspyhf-git airspy-git rtaudio)
pkgver() {
  cd "SDRPlusPlus"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}
prepare() {
  true
}
