
_suffix="-v3"
pkgname=("${pkgbase}${_suffix}" "${pkgbase}-headers${_suffix}")
for _p in "${pkgname[@]}"; do
  local _q="${_p%$suffix}"
  eval "package_$_p() {
    $(declare -f "_package${_q#$pkgbase}")
    _package${_q#$pkgbase}
  }"
done
