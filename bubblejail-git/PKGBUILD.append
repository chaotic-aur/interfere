
pkgver() {
  cd "$srcdir/$pkgname"

  git describe --long --tags --exclude='*[a-zA-Z][a-zA-Z]*' \
    | sed -E 's/^v//;s/([^-]*-g)/r\1/;s/-/./g'
}

prepare() {
  sed -Ei -e "s@^\s+'--urgency', 'critical',.*\$@@" \
    "$srcdir/$pkgname/src/bubblejail/bubblejail_cli.py"
}
